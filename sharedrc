# Todo.txt
#alias t="$HOME/dotfiles/bash/todo/todo.sh -d $HOME/dotfiles/bash/todo/todo.cfg"
#source "$HOME/dotfiles/bash/todo/todo_completion"
#complete -F _todo t

# JSON Parsing shortcut
alias pp="python -m json.tool"

# Android SDK Stuff
alias android_emulator="$HOME/android/sdk/tools/emulator"

# Custom Command Line Programs
export PATH="${HOME}/dotfiles/bin:${PATH}"

# Brew Python
#export PATH="/usr/local/share/python:${PATH}"

# RVM
# Load RVM into a shell session *as a function*
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

# Boxen
[[ -s /opt/boxen/env.sh ]] && source /opt/boxen/env.sh

# Source our local file, for stuff we don't want to share
#if [ -f ~/.local_bashrc ]; then
#  source ~/.local_bashrc
#fi
# Easier way to right the above snippet
[[ -s ~/.local_bashrc ]] && . ~/.local_bashrc

# Add RVM to PATH for scripting
PATH=$PATH:$HOME/.rvm/bin

# Add homebrew paths
PATH=/usr/local/bin:/usr/local/sbin:$PATH

# Add npm to PATH for scripting
PATH=/usr/local/share/npm/bin:$PATH

# Add macvim as vim
#alias vim=/usr/local/Cellar/macvim/7.3-65/MacVim.app/Contents/MacOS/macvim
